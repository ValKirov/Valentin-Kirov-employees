export type CsvRow = {
    EmpID: string;
    ProjectID: string;
    DateFrom: string;
    DateTo: string;
};

export type Result = {
    FirstEmpId: string;
    SecondEmpId: string;
    days: string;
};